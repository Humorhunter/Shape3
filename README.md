# Shape3 · 三形阵

一款双人热座（本地对战）的抽象策略游戏。双方各持一个 3×3 的阵地，用三种图形排兵布阵，隐藏信息，回合制对抗。

## 规则

三种图形各有分工：

| 图形 | 作用 |
| --- | --- |
| 圆 (Circle) | 防御：被进攻时优先消耗，保护生产；未被摧毁则跨回合保留 |
| 三角 (Triangle) | 进攻：每回合必发射、寿命一轮，只摧毁敌方「圆形 → 方形」，不攻击敌方三角 |
| 方 (Square) | 生产：结算后剩余方形数 = 下一回合可放置的图形数 |

流程：

1. **初始布阵**：双方各自秘密放置 9 个图形（任意组合），互不可见。
2. **放置**：每回合可放置数量 = 上一回合结算后的方形数；每个格子可堆叠多个兵力（每格上限可配置，默认 9）。
3. **进攻**：双方同时结算；每个三角摧毁一个敌方图形（圆形优先，其次方形），三角自身必毁。
4. **结算**：统计方形数，作为下一回合生产；判定胜负。

胜负：

- **歼灭模式（默认）**：一方兵力归零判负，可平局。
- **固定回合模式（可选）**：打满 N 回合后，先比方形数，再比总图形数。

## 技术栈

- TypeScript + Vite + Canvas
- Vitest（规则引擎单元测试）

## 本地运行

```bash
npm install
npm run dev      # 开发服务器
npm run build    # 类型检查 + 构建到 dist/
npm run test     # 运行单元测试
npm run preview  # 预览构建产物
```

## 目录结构

```
src/
├─ game/
│  ├─ types.ts      类型定义
│  ├─ constants.ts  常量
│  ├─ engine.ts     纯函数规则引擎
│  └─ state.ts      回合状态机
├─ ui/
│  ├─ render.ts     Canvas 绘制
│  └─ board.ts      棋盘视图与命中检测
├─ styles.css
└─ main.ts          入口与游戏编排
tests/
├─ engine.test.ts   规则引擎测试
└─ state.test.ts    状态机测试
```

## 后续可扩展

- 单人对战 AI
- 在线联机
- 阵型预设 / 局内动画增强
