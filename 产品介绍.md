# Shape3 · 三形阵 产品说明

## 产品介绍（500 字以内）

Shape3·三形阵是一款回合制抽象策略游戏。双方各据一个 3×3 阵地，用三种几何图形排兵布阵、互相看不到对方布局：圆形负责防御、三角形负责进攻、正方形代表生产力。

初始双方秘密放置 9 个图形，随后进入「布置—战斗—结算」循环：每回合可放置的兵力数等于上一回合结算后的正方形数；战斗时双方同时开火，每个格子里的三角形直线攻击对方同一位置的格子，优先摧毁圆形、再摧毁正方形，三角攻击后自身必毁，若对应格无目标则自毁；圆形则在被攻击时优先消耗、用于保护生产力。当一方兵力归零即判负，也可选择固定回合数后比较正方形与总兵力。

游戏支持双人热座（换手遮挡屏）与单人挑战机器人两种模式，并配有战斗动画、战斗重播，以及可配置的「每格兵力上限」等参数。规则简洁直观、上手快，但位置对应的攻击机制与隐藏布阵让策略空间丰富。

## 技术与应用（300 字以内）

项目采用 TypeScript + Vite + Canvas 构建，规则逻辑与界面渲染彻底分离：`engine.ts` 为纯函数规则引擎，`state.ts` 管理回合状态机，`ai.ts` 实现简易 AI，`render.ts` / `board.ts` 负责 Canvas 绘制与命中检测，`main.ts` 负责编排与动画；并用 Vitest 编写 37 个单元测试保证规则正确。开发中遇到的主要难题：1）隐藏信息的双人热座流程，通过「换手遮挡屏」解决；2）Canvas 首帧尺寸未初始化导致棋盘不可见，以及工具栏变化引发的画布缩放抖动，通过「先同步工具栏、再同步画布尺寸、再布局」的渲染顺序解决；3）战斗动画与状态解耦，用 requestAnimationFrame 逐帧驱动，并以 `preBoards` 保存战前快照实现重播。

## 创新与优势（300 字以内）

1. 规则新颖：将「生产 / 进攻 / 防御」映射为三种几何图形，直观且富有辨识度；
2. 位置对应的攻击机制：每个格子只攻击对方同位置格子，强调布阵与预判，空置即自毁，惩罚冒进；
3. 同格可堆叠多兵力且上限可配置，带来不同节奏与深度的玩法；
4. 架构清晰：纯函数引擎 + 状态机 + UI 分层，便于快速扩展 AI 难度、在线联机与更多模式；
5. 体验完整：内置简易 AI、战斗动画与重播，开箱即玩、便于演示。
