# Shape3 · 三形阵 平衡分析

## 一、单位价值模型

所有单位的生产成本相同：1 次放置（1 个正方形产出）。衡量「性价比」时，把一切都折算成「放置次数」这一货币。

- **正方形（生产）**：每回合产出 1 次放置，且可跨回合累积。设每回合存活概率为 p，则一个正方形的期望产出为等比级数 `p + p² + p³ + … = p/(1-p)`。它是「资产型」单位，活得越久越值钱。
- **三角形（进攻）**：寿命固定 1 回合，价值 = 它摧毁的敌方单位价值 − 自身成本 1。
- **圆形（防御）**：被攻击时优先消耗、1:1 抵挡三角，价值 = 它保护下来的单位价值；未被攻击则跨回合保留。

## 二、交换率（1 次放置的成本下）

| 交换 | 进攻方投入 | 防守方损失 | 净收益 |
| --- | --- | --- | --- |
| 1 三角 → 1 圆 | 1 | 1 | 0（平换，但圆可留存） |
| 1 三角 → 2 方 | 1 | 2 | +1（三角克制方） |
| 1 三角 → 空格 | 1 | 0 | −1（自毁，惩罚盲攻） |
| 1 圆抵挡 1 三角 | 1 | 保护约 2 方 | 间接 +1（圆克制三角） |

以中等存活率 p = 2/3 估算，1 个正方形终身期望产出 = `(2/3)/(1/3) = 2` 次放置；1 个三角摧毁 2 个正方形，等于摧毁敌方约 4 次放置的产能，扣除自身成本 1 后净收益约 +3，但前提是「猜中对方布阵、且未被圆形拦截」。

## 三、平衡结论

由上述交换率可构成一个稳定的剪刀石头布循环：

- **三角形克制正方形**（1:2，效率高）；
- **圆形克制三角形**（1:1 抵挡，且圆形可留存，被拦截的三角血本无归）；
- **正方形克制圆形**（经济滚雪球：只堆圆没有产出，长期必然落后）。

三者没有严格占优单位，胜负取决于**布阵比例与位置预判**。因此当前参数组合达到平衡：

- `SQUARES_PER_TRIANGLE = 2`（1 三角摧毁 2 方）
- `CIRCLES_PER_TRIANGLE = 1`（1 圆抵挡 1 三角）

这两个比值都做成了 `constants.ts` 中的可配置参数，便于后续微调。若实测发现「正方形过弱」，可调小 `SQUARES_PER_TRIANGLE`、或把每方产出提高到 2；若「进攻过强」，可提高 `CIRCLES_PER_TRIANGLE` 让圆抵挡更多三角。
